# Build-time settings for the site. Copy to .env for local development.
# Production builds leave both unset and get simhook.dev's addresses.

# Where the API lives. The site only calls it to sign a visitor out.
PUBLIC_API_URL=http://localhost:8080

# Where the dashboard lives, for the Sign in and Dashboard links in the bar.
PUBLIC_APP_URL=http://localhost:3000
